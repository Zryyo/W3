
  # W3

  This is a code bundle for W3. The original project is available at https://www.figma.com/design/fhMxfqsmws4hEaPC1SdRNb/W3.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  